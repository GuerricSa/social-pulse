<div id="message-<%= message.id %>">
  <small>
    <strong><%= message.user.first_name %></strong>
    <i><%= message.created_at.strftime("%e %b à %kh%M") %></i>
  </small>
  <p><%= message.content %></p>
</div>
